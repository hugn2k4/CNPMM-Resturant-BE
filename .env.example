# Server Configuration
PORT=8080
NODE_ENV=development
APP_NAME=express-be-skeleton
LOG_LEVEL=info

# Database Configuration
MONGO_URI=mongodb://127.0.0.1:27017/node_fulltask
# For MongoDB Atlas (cloud):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ACCESS_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=86400000

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
# For multiple origins:
# CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Email/SMTP Configuration (for sending OTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=nhatminhtranchuyenly@gmail.com
SMTP_PASS=gyrx xipf zbfi lbro
FROM_EMAIL=nhatminhtranchuyenly@gmail.com
FROM_NAME=CNPMM

# Optional: For Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" at: https://myaccount.google.com/apppasswords
# 3. Use that app password as SMTP_PASS

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Session/Cookie Settings
COOKIE_SECRET=your-cookie-secret-key-change-this
SESSION_MAX_AGE=86400000

# Optional: Redis (if you want to use Redis for sessions/cache)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Optional: File Upload
# MAX_FILE_SIZE=10485760
# UPLOAD_PATH=./uploads

# Optional: Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173